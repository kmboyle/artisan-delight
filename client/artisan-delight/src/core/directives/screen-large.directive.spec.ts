import { ScreenLargeDirective } from './screen-large.directive';

describe('ScreenLargeDirective', () => {
  it('should create an instance', () => {
    const directive = new ScreenLargeDirective();
    expect(directive).toBeTruthy();
  });
});
